name: Update Scoop Manifests

on:
  schedule:
    - cron: '0 0/3 * * *'
  workflow_dispatch:

jobs:
  scoop-checkver:
    runs-on: windows-latest
    
    permissions:
      contents: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install Scoop
        run: |
          Invoke-Expression (New-Object System.Net.WebClient).DownloadString('https://get.scoop.sh')
          scoop install git

      - name: Add Local Bucket & Update Manifest
        run: |
          scoop bucket add local .
          scoop checkver * -u -dir .

      - name: Commit and Push Changes
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          
          if (git status --porcelain) {
            git add freecad-weekly.json
            git commit -m "Auto-update: FreeCAD Weekly"
            git push
          } else {
            Write-Host "Keine Updates gefunden."
          }